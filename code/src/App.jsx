import TodayView from './views/TodayView/TodayView.jsx'

export default function App() {
  // Phase 1: Today is the only view rendered. View switching (menu pill,
  // Photo Slideshow, Now Playing, Settings) lands in a later task.
  return <TodayView />
}
